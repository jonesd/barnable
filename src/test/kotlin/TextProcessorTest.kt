import org.junit.jupiter.api.Assertions.*
import org.junit.jupiter.api.Test

class TextProcessorTest {
    val source = "Recent protectionist measures by the Reagan administration have disappointed us. "

    @Test
    fun `Build up sentences from source`() {
        // fixem val textModel = TextModelBuilder(source).buildModel()
        // fixme TextProcessor(textModel).runProcessor()
    }
}